# Privacy Policy — template pointer

Authoritative structured copy lives in **`src/config/legalContent.ts`** (library key `privacy` per niche).

Interpolated fields come from **`siteConfig.legal`**, **`brand.displayName`**, and **`location.phone`**.
